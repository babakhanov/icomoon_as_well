#!/usr/bin/env ruby

require "icomoon_as_well"
require "byebug"
include Byebug

if File.exist?(ARGV[0].to_s) && ARGV[0].to_s =~ /^.+\.zip$/ && Dir.exist?(ARGV[1].to_s)
  icomoon = IcomoonAsWell::Icomoon.new(ARGV[0], ARGV[1])
end



icomoon.say_hello

